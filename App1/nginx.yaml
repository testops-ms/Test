apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: ng-app
  name: ngin-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ng-app
  template:
    metadata:
      labels:
        app: ng-app
    spec:
      containers:
        - image: nginx
          name: ng-app
